# Environment Variables Configuration
# For full documentation, see: src/utils/processEnv.ts
#
# File structure:
#   .env.dev              - Development configuration
#   .env.staging          - Staging configuration (production build with VITE_STAGING=true)
#   .env.production       - Production configuration
#
# How it works:
#   - `yarn dev` uses .env.dev (Vite loads .env.dev automatically in dev mode)
#   - `yarn build:staging` and `yarn build:production` use env-cmd to load respective env files
#   - quasar.config.ts generates env object from schema keys (single source of truth)
#   - Build-time Vite plugin validates all env vars before build proceeds

# Required variables
VITE_API_BASE_URL="http://localhost:8084"
VITE_BACK_DID="did:web:localhost%3A8084"
VITE_IS_ORG_IMPORT_ONLY="false"

# Optional variables
# VITE_DEV_AUTHORIZED_PHONES="+33612345678,+12345678910"  # Dev/staging only - must not be in production
# VITE_DISCORD_LINK="https://discord.gg/example"  # Discord invite link for support
# VITE_CONVERSATION_EXPORT_ENABLED="true"  # Enable/disable conversation export feature (must match backend)
